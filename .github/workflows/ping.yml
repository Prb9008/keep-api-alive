name: Keep APIs Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # Runs every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call API 1
        run: curl ${{ secrets.API_1_URL }}

      - name: Call API 2
        run: curl ${{ secrets.API_2_URL }}

      - name: Call API 3
        run: curl ${{ secrets.API_3_URL }}

      - name: Call API 4
        run: curl ${{ secrets.API_4_URL }}
